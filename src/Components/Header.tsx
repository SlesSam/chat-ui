import { Box,Text } from "@chakra-ui/react";

const Header = () => {
    return (
        <Box alignSelf="center">
          <Text fontSize="3xl" fontWeight="bold">
            Chat golang with lambda apigateway
          </Text>
        </Box>
      );
}

export default Header;